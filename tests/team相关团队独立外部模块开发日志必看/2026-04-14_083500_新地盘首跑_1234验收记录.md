# 2026-04-14 新地盘首跑：1 / 2 / 3 / 4 验收记录

## 背景

我们把完整源码包复制到新的运行沙盒：

- `review-bundles/2026-04-14_runcheck-sandbox`

现在这轮首跑也已经有统一重放命令：

```bash
node scripts/migration-rehearsal.mjs --with-install --with-seed --with-run
```

它会把 stdout / stderr / runtime file 都写进：

- `review-bundles/2026-04-14_runcheck-sandbox/migration-run.stdout.log`
- `review-bundles/2026-04-14_runcheck-sandbox/migration-run.stderr.log`
- `review-bundles/2026-04-14_runcheck-sandbox/.dev-runtime.json`

目的不是继续在旧地盘里猜，
而是直接验证：

1. 能不能运行
2. 有没有丢失
3. 解耦情况
4. 旧东西是否也一起被看清

## 1. 能不能运行

### 结果

**能运行，而且第七跑已经通过“带配置 + 路径重写 + 公共 export 协议”的真实运行验证。**

补充：

- 上面提到的“第七跑”，现在已经可以直接用
  `node scripts/migration-rehearsal.mjs --with-install --with-seed --with-run`
  重放同类验证，不需要再手工搬包、安装、起服务、等 `/health`。

### 证据

- backend 成功启动
- `/health` 返回 `200`
- frontend 成功起到：
  - 第一跑自动避让到 `http://127.0.0.1:5178/`
  - 第三跑已正常回到 `http://127.0.0.1:5176/`
  - 第四跑继续稳定保持 `http://127.0.0.1:5176/`
  - 第五跑带角色配置种子继续稳定
  - 第六跑在路径重写后依旧稳定保持 `http://127.0.0.1:5176/`
  - 第七跑继续稳定保持 `http://127.0.0.1:5176/`

## 2. 有没有丢失

### 第一次运行时暴露的丢失

第一次启动失败时，
直接报：

```text
Cannot find module '../../clean-room/spine/modules/inbound'
```

### 结论

这说明：

- `clean-room/` 是现役依赖
- 不能从完整包里省掉

后续补进 `clean-room/` 后，
主链就能起来。

### 当前没有丢的部分

以下关键现役内容都已经跟着走过去：

- `src/`
- `server/`
- `scripts/`
- `public/`
- `SKILLs/`
- `clean-room/`
- 核心根配置文件

## 3. 解耦情况

### 已经成立的解耦

新地盘启动后，
服务端打印出的：

- `Workspace`
- `Runtime root`
- `User data path`

都已经指向新地盘自身。

这说明：

- 路径锚点主线是成立的
- 运行态不会再偷偷回旧地盘

### 第二到第七跑进一步暴露并修掉的点

第二跑时暴露：

- `DailyMemory` 已不再硬报错，
  但会留下：
  - `Slot 未标记完成`
- 角色运行态补种子逻辑只补到一半，
  还因为漏导入报：
  - `syncRoleCapabilitySnapshots is not defined`

第三跑后状态变成：

- `DailyMemory` 只留下温柔提示，不再硬崩
- `P0-RUNTIME-FILE-SELF-CHECK ok`

第四跑时进一步确认：

- backend 正常
- frontend 正常
- `P0-RUNTIME-FILE-SELF-CHECK ok`
- `DailyMemory` 只留下：
  - `Slot 2026-04-13 未标记完成：本次未成功写入长期记忆，保留后续补跑机会`

第五跑时进一步确认：

- 新地盘带上最小角色配置种子后，`DailyMemory` 已经真正进入执行
- 但同时暴露：
  - `conversationFileCache.directory` 仍指向旧地盘绝对路径
  - 所以 backup 被写回旧地盘

第六跑时进一步确认：

- 已通过脚本把 `conversationFileCache.directory` 从旧地盘重写到新地盘
- 启动日志变成：
  - `Startup catch-up not needed (last=2026-04-13, target=2026-04-13)`
- 没再出现“写回旧地盘 backup 路径”的现象
- `P0-RUNTIME-FILE-SELF-CHECK ok`

第七跑时进一步确认：

- 公共 export 状态协议在新地盘可用
- `GET /api/role-runtime/writer/exports`
- `GET /api/role-runtime/organizer/exports`
  都返回 `success=true`
- 返回的 `exportStatus.roots.primary`
  和 `legacy`
  都已经指向新地盘自己的 `uploud/.cowork-temp/attachments/...`
- 不再指向旧地盘

### 结论

所以当前状态已经推进到：

- **源码运行主链已解耦**
- **角色运行态最小初始化种子已能自动补齐**
- **`DailyMemory` 在空配置新地盘下已变成温柔跳过**
- **路径型配置也已经能重写到新地盘**
- **公共 export 状态协议在新地盘也成立**
- **第七跑已经通过“带配置 + 路径重写 + export 协议”的真实迁移验证**

也就是：

新地盘已经不只是“能起的净新壳”，
而是已经接近“完整迁移后的可运行家园”。

## 4. 旧东西是否也一起被看清

### 结果

**看清了，而且非常有效。**

这次新地盘验证帮我们确认了：

- 哪些是“看起来像旧目录，其实现役还依赖”的东西
  - 例如 `clean-room/`
- 哪些是“路径主线已经收住”的东西
  - 例如 `projectRoot / runtimeRoot / userDataPath`
- 哪些是“代码能起，但配置会带回旧地盘”的东西
  - 例如 `conversationFileCache.directory`
- 哪些是“新协议是否跟得过去”的东西
  - 例如 `exportStatus`

### 结论

旧东西不是“看名字判断删不删”，
而是：

- 搬到新地盘
- 让程序自己说真话

这样最清楚。

## 本轮总评

### 1 / 2 / 3 / 4 总结

1. **能不能运行**
   - 能
   - 第七跑已通过“带配置 + 路径重写 + export 协议”的真实迁移验证

2. **有没有丢失**
   - 有，最初少了 `clean-room/`
   - 已补回

3. **解耦情况**
   - 路径主线已解耦
   - 角色运行态最小初始化已能自动补齐
   - `DailyMemory` 空配置场景已温柔降级
   - 路径型配置已经能重写到新地盘，不再把结果写回旧地盘
   - export 公共协议也已经跟着迁移过去

4. **旧东西是否被一起看清**
   - 是，而且这是这轮最有价值的结果

## 下一步

接下来最应该补的不是继续猜，
而是：

1. 把“最小角色配置种子”正式纳入迁移流程
2. 把“配置路径重写”正式纳入迁移流程
3. 把“完整包 -> 新地盘 -> 多轮起跑验收”正式纳入迁移流程

这样下次就不是临时经验，
而是稳定方法。
