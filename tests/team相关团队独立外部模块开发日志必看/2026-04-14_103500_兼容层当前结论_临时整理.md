# 兼容层当前结论（临时整理）

## 现在不能硬删的

1. legacyAttachmentManualAbs / legacyAttachmentExportsAbs
原因：旧的 <role>/attachments/manual|exports 目录里还有历史文件要靠它们兜底读。

2. remote_httpSessionExecutor 里的同套 legacy fallback
原因：远程执行器仍需与本地执行器保持同一份旧文件读取能力。

3. exportVerification 里的 roots.legacy 和 legacy entries
原因：旧 exports 目录里的结果还需要被状态接口看见。

## 现在可以继续减的

1. role_home_paths 不再对外返回 legacy_* 门牌
状态：已做。

2. runRoleHomeFilesTool 里 legacyEntries 只算不回给 agent
状态：已从对外输出撤掉，但代码里仍在计算，可继续减。

3. exportVerification 里 legacy entries 可改成仅在 legacy 根下真的有文件时才枚举
状态：可做下一刀。

4. attachmentRuntime 的 anchored truth 已成立，可以把旧“绝对路径也行”的心智彻底清掉
状态：已做一层，还可继续巡检相关说明。
