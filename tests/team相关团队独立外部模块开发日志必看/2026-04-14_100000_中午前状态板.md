# 2026-04-14 中午前状态板

## 当前总状态

主家已经从“抢修主链”进入“清尾巴 / 做收口”的阶段。

不是所有问题都没了，
但大的洞已经基本补住了。

## 已完成

### 1. 主链稳定性

- 路径锚点主线已收住
- `dialog` / 附件 / 房间 / export 核验主线已成立
- `Room` 不再静默失败
- `Room` 已有最小导出核验闭环
- 公共 export 状态协议已建立

### 2. 契约与路标

- 核心字段契约已对齐
- 关键旧路标已更新到现役口径
- agent 房间里的工具顺序说明已经比以前清楚很多

### 3. 新地盘迁移验证

已经跑到第七轮，
并通过：

- 能启动
- 不牵旧地盘
- 角色运行态文件能自动补齐
- `DailyMemory` 能温柔降级
- export 公共协议能在新地盘正常返回
- `role_home_write_file` 能真写出文件

### 4. team 编译挡板

- `SingleTaskRunnerPage.tsx` 的编译挡板已修
- 全仓 `tsc` 已能通过

### 5. 根目录噪音

已经安全归档：

- `package-0lock.json`
- `.tmp-deploy-sg.ps1`

## 还剩的小尾巴

### A. 兼容层还没完全瘦身

主要是：

- `legacyAttachment*`
- 一些 legacy compat 注释和保底路径

现在已经不再直接误导 agent，
但代码本体还没继续做深一层减法。

### B. 迁移流程还没完全工具化

现在已经有：

- 检查包
- 完整源码包
- 配置路径重写脚本
- 多轮新地盘验收经验

但还没有收成“一套正式迁移命令链”。

### C. 根目录里 `team*` 还在

包括：

- `team.html`
- `team_page.html`
- `team_bundle.js`

当前先不动，
因为它们还是委托项目的现役入口壳。

## 中午前不碰

### 1. 不动 team 入口结构

原因：

- `team` 是近两天可能要交付的委托
- 现在不能为了主家整洁去动它的入口壳

### 2. 不开新功能

不做：

- Office 真执行桥
- zip
- 24h 清理
- 大规模 UI 重构

### 3. 不做全仓大扫除

只收最明确的小尾巴，
不再把旧壳清扫扩成一场新工程。

## 中午前如果还有时间

优先顺序：

1. 继续做兼容层减法
2. 把迁移方法整理成更明确的操作链
3. 再补一页“我们现在已经到哪”的对外/对内说明

## 一句话判断

现在已经不是“家园坏掉、四处漏风”的状态了。

现在是：

- 主梁立住了
- 真问题看清了
- 新地盘也跑通了
- 剩下的是收尾和减法

所以中午前，
我们是来得及拿出一个“清楚、稳定、可继续工作”的状态面的。
