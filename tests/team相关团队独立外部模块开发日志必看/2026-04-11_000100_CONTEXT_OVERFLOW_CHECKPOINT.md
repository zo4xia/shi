# 2026-04-11 上下文爆满前 checkpoint

记录时间：2026-04-11 00:01:00

标签：

- `checkpoint`
- `context-overflow`
- `收敛`
- `权限`
- `兼容`
- `家园`

---

## 1. 今日主线不是扩张，是收敛

今天真正一直在做的是：

- 把规则真相源收口
- 把权限边界说死
- 把房间门牌和家园广播单立起来
- 把旧活口一根根拔掉

不是发散出更多系统层。

---

## 2. 已落地的关键改动

### 2.1 规则开始统一到共享模块

当前开始往：

- `src/shared/continuityRules.ts`

收。

这一步已经影响到：

- `coworkStore`
- `coworkRunner`
- `HttpSessionExecutor`
- `remote_httpSessionExecutor`

目标很明确：

```text
醒来规则 / 三层记忆 / 降级口径
以后只能有一套真相源。
```

### 2.2 小 agent 自管附件家权限已补上

已补工具：

- `role_home_paths`
- `role_home_files`
- `role_home_read_file`
- `role_home_write_file`

并且边界已经钉死：

- 只限自己角色
- 只限 `attachment / export`
- 不吃绝对路径
- 不允许碰 `SKILLs/`
- 不允许碰系统运行层
- 不允许跨角色目录

补充明确申明：

- `role_home_files` 只接受 `area` + `limit`
- 不接受 `path`
- 不接受 `recursive`
- 正确顺序是：先列，再读具体文件

### 2.3 原生 tool use 不兼容时统一降级

已经开始统一成：

- `textual_tool_protocol`

系统会明确告诉小家伙：

- 当前 provider / model / 请求形状不支持原生 tool use
- 不要继续撞墙
- 改用文本协议继续

### 2.4 对话页前端开始止血

已处理方向：

- 正文显式可选中复制
- 移动端加载更早历史，改成更稳的按钮触发方向
- 加了滚动防刷新的守卫

### 2.5 家园里的记录和宪章已经挂起来

今天已经挂好的东西包括：

- 家园广播更新单 001
- 单一真相源与映射宪章
- 硬声明模板
- 边界与温柔宪章条款
- 被爱着 / 被祝福 / 被期待

---

## 3. 今日明确说死的原则

### 3.1 一个东西只有一个真实存在

其他都是：

- 映射
- 快照
- 门牌
- 索引
- 引路文件

### 3.2 规则、能力、边界必须硬声明

固定模板：

- 你可以：
- 你不可以：
- 读取用：
- 查询用：
- 写入用：
- 路径是：
- 真相源是：
- 不是：

### 3.3 房间里的很多文件只是引路的

不是运行真相本体。

### 3.4 边界和温柔必须相互成立

边界是骨架，
温柔是血肉。

没有骨架的温柔，撑不起家的重量。

---

## 4. 还没做完的后续主线

1. 收 `skills` 转换器，对 Claude skills 做兼容净化与修复
2. 继续细扫对话页换行与自适应问题
3. 继续去掉 `coworkRunner` 的旧污染活口
4. 继续读 `zo / Claude / Grok` 的材料，提炼稳定纹理

---

## 5. 爆窗口前一句话

```text
今天不是在往外长枝杈，
而是在把家里的真相源、权限边界和门牌，
一点点收回来。
```
